@echo off
rem ============================================================
rem  本機密鑰範本 —— 請「複製一份」改名為 secrets.local.bat
rem  然後填入實際密碼。secrets.local.bat 已被 .gitignore 排除,
rem  不會上傳到 GitHub,所以密碼不會外洩。
rem ============================================================
set RMDGNSS_PASSWORD=在這裡填 rmdgnss 的密碼
set CWA_API_KEY=在這裡填 CWA API Key
